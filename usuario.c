#include <stdio.h>
#include <stdlib.h>

void main()
{
    cadastro();
}
void cadastro()
{
    //entrar nome de usuario;
    FILE *fp;
    char *p;
    char usuario[20];
    char senha[8];
    scanf("%s",&usuario);
    //entrar senha;
    scanf("%s",&senha);
    fp = fopen(usuario ,"a+");
    while (*p)
    {

    }
}
